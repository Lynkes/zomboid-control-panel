# Modernization Risk Register

Scoring: likelihood 1-5, impact 1-5, score = likelihood x impact.

| Score | Severity |
| --- | --- |
| 20-25 | critical |
| 12-19 | high |
| 6-11 | medium |
| 1-5 | low |

| Risk ID | Description | Likelihood | Impact | Score | Severity | Owner | Linked WP | Mitigation | Trigger/Signal | Status |
| --- | --- | ---: | ---: | ---: | --- | --- | --- | --- | --- | --- |
| RISK-001 **[DIAGNOSED - cause found, fix not applied]** | `server/tests/bugfixes.test.js` "forwards ordinary Say chat" has a hard 5000 ms timeout and fails on a **cold** first run of the full server suite, then passes warm. Observed twice on the unmodified baseline (`8642dc4`): cold run 534/535 exit 1 with import cost 86.13 s; warm run 535/535 exit 0 with import cost 35.40 s; the file alone passes 93/93 in 3.27 s with import 1.39 s. The baseline is green but **not deterministic on first run**. | 4 | 4 | 16 | high | coordinator | FND-001 (found), FND-002 (gate consumer) | **CAUSE FOUND (independent investigation, 2026-08-22): verdict (A), a tight timeout, NOT a race.** `bugfixes.test.js:658` does `await import("../services/discordBot.js")` INSIDE the test body. That module pulls in discord.js (478 files / 4.2 MB) plus six @discordjs packages and undici. Because the import is dynamic and memoised per specifier, **only the first caller pays the cold transform cost - and the first caller is the failing test at line 671**, which absorbs it inside its own 5000 ms budget. Confirmed on a WARM box: that one test takes 1488 ms against 0-5 ms for every neighbour in the same file, a 300-1000x outlier that is purely import-bound. (B) was ruled out ON CODE GROUNDS, not merely by failure to reproduce: `handleGameChat` (discordBot.js:228-281) was read end to end - no setTimeout, no retry, no circuit breaker, no fake timers, no network. Reproduction was 0/3 today because the box was already warm; the real cold/warm factor is OS page cache, not vitest's own cache (deleting node_modules/.vite changed nothing). **Recommended fix, NOT applied: hoist the import into a one-time `beforeAll` for that describe block and its 10 sibling call sites**, moving the cost out of the per-test window onto collection. Explicitly NOT recommended: bumping this one test's timeout (treats the symptom) or raising the global default (would mask genuine slow-test regressions suite-wide). | A red server suite whose only failure is this test, especially on a fresh checkout or clean CI runner | **resolved by FND-007** - the import is now static; the test runs in 1 ms against 1488 ms before |
| RISK-002 | `data/db.example.json` is stale against the authoritative schema in `server/database/init.js` `defaultData` (lines 57-76). It carries 3 keys the schema does not define (`activity_log`, `mods`, `active_server_id`) and omits 14 that it does. | 3 | 4 | 12 | high | coordinator | DB-001, DB-003 | Treat `defaultData` (19 collections) as the only schema authority. Derive all synthetic fixtures from it. Do not seed the importer or parity comparator from the example file. The plan's V1 Collection Mapping table already matches `defaultData` exactly, so the plan is right and the example file is wrong. | Any fixture, importer test, or parity report referencing `activity_log`, `mods`, or `active_server_id`, or covering fewer than 19 collections | open |
| RISK-003 | The FND-001 performance baseline covers exactly one route: `GET /api/auth/status`, unauthenticated and non-I/O. Authenticated, filesystem, RCON, and PanelBridge routes have no recorded p50/p95. | 4 | 3 | 12 | high | coordinator | FND-001, FND-002 | Anchor only the plan's "contract validation adds no more than 5 ms p95" budget to this number. Before any lifecycle or persistence work claims a performance comparison, extend `measure-baseline.mjs` coverage to representative authenticated and I/O-bound routes. State the gap rather than implying broad coverage. | Any claim that a change "did not regress performance" that cites only PERF.json | open |
| RISK-004 | Client production build emits a >500 kB post-minification chunk advisory, and no byte figure has been recorded. The plan requires per-phase measurement of English bundle growth. | 3 | 2 | 6 | medium | coordinator | I18N-001 | Record the actual `client/dist` byte size before locale resources are introduced, so growth is measured against a real number rather than an advisory string. | Locale namespaces added with no prior recorded bundle size | open |
| RISK-005 | Worktrees are prohibited until a user-authorized local checkpoint commit exists, because untracked handoff files do not appear in a new worktree. Until then FND-002, FND-003, and DB-001 cannot run in parallel and the program is serialized through the coordinator. | 5 | 2 | 10 | medium | coordinator | FND-002, FND-003, DB-001 | Request the checkpoint commit at the FND-001 boundary, which is exactly where the plan places it. Do not create a worktree, branch, or commit before that authorization. | Any attempt to run `create-worktree.ps1` while `git status` still shows the handoff files untracked | **resolved** by user-authorized local checkpoint `e966fe9`; handoff files are now tracked |
| RISK-006 | The mandatory baseline gate `npm run test:server` writes `data/db.json`, `data/backups/*.json`, and `logs/` into the repository, and `bootstrap-plan.ps1` then throws `data/db.json must not exist in the modernization fork baseline.` **The plan's own documented FND-001 sequence cannot be run twice without a manual cleanup.** Hit for real during FND-001 and reproduced independently by the verifier. Content was the empty default DB (identical SHA-256 across all six generated JSON files); no data was at risk and no secret was written. | 5 | 3 | 15 | high | coordinator | FND-001 (found), FND-002 onward (every future gate) | Full analysis and four remediation options recorded as DISC-001 in `DECISIONS.md`; recommendation is to isolate the test suite's data root the way the perf step already isolates its own via `paths.config.json`. Interim: `README.md` documents the cleanup so the throw is self-explaining instead of alarming. **Awaiting user decision — not yet fixed.** | `bootstrap-plan.ps1` throwing on `data/db.json` immediately after a green gate | **resolved by FND-005** - a second consecutive gate run followed by the preflight now returns PASS, and the suite still reports 535 tests so discovery was not altered |
| RISK-007 | `check-owned-paths.ps1` invoked the way the plan documents it (`pwsh -File ... -AllowedPath a,b`) **binds the comma list as a single string, so every allowed path is silently discarded**. Proven directly: `-File ... -AllowedPath a,b,c` yields `elements=1` containing `"a,b,c"`. FND-001's check returned PASS only because its paths coincide with the script's hardcoded `$initialHandoff` fallback - the argument was never evaluated. The first package whose paths fall outside that fallback is the first to get a wrong answer, and by then the guard has a history of passing. | 5 | 4 | 20 | critical | coordinator | every package from FND-005 onward | Recorded as DISC-002 with three options. Correct form is `pwsh -Command "& './scripts/modernization/check-owned-paths.ps1' -Id X -AllowedPath @('a','b')"`. FND-005's scope was re-verified with the correct form and passes at changed=12. **Plan text not yet corrected; script not yet hardened.** | A `PASS` from this script for a package owning paths outside `docs/modernization/`, `scripts/modernization/`, `AGENTS.md`, or the plan file | **resolved by FND-006** - the script splits on commas and now THROWS on an argument yielding no usable entries; proven with an accept case, a refuse case, and a still-catches-a-real-violation case |
| RISK-008 | The DISC-001 isolation **deliberately does not engage when a `paths.config.json` already exists** - it logs and leaves the file untouched rather than clobbering a developer's real data-root override. Isolation is therefore conditional, not absolute. | 2 | 3 | 6 | medium | coordinator | FND-005 | Intended trade: silently replacing a real override would be worse than the defect fixed. The setup prints which mode it took, so the condition is observable rather than silent. | `data/db.json` reappearing after a gate run, together with the setup's "leaving it untouched" log line | **resolved by 48cbf53** - the conditionality this row describes only existed because every process checked ONE shared repo-root `paths.config.json`. `PANEL_PATHS_CONFIG_PATH` plus a per-run config file inside its own temp directory means a test run has its own path and never consults the repo-root file at all, so there is no longer a pre-existing file for the isolation to decline against: isolation is unconditional for test runs. The real panel never sets the var and still reads the repo-root file exactly as before, so the trade this row protected - never clobber a developer's real override - is preserved rather than abandoned. Confirmed independently from the commit by a second agent, not carried over from RISK-009's own note. |
| RISK-009 | **Sharpened by the independent verifier and it is worse than RISK-008 alone states:** if a pre-existing `paths.config.json` points at a *real* data root, `npm run test:server` will read and write that **live** `db.json` under test. FND-005's safety branch deliberately declines to override it, so the isolation does not engage precisely when the stakes are highest. | 2 | 5 | 10 | medium | coordinator | pre-existing; not introduced by FND-005 | **This predates FND-005 and no test-harness change can close it** - the harness cannot distinguish a developer's deliberate override from a dangerous one without guessing. Mitigation is procedural: never point `paths.config.json` at a live data root on a machine where the suite is run. A future option is a explicit opt-in env flag that forces isolation regardless, but that is a new decision, not a bug fix. | A `paths.config.json` present at the repo root whose `dataDir` is not under a temp path | **resolved** - the "future option" this row named is now the fix. `PANEL_PATHS_CONFIG_PATH` (`server/utils/paths.js`) plus a per-run isolated config file inside its own temp directory (`server/tests/vitest.globalSetup.mjs`) means every test run gets its own file and never touches the repo-root `paths.config.json` at all, so there is no shared path left to guess about - deliberate override or dangerous one, it is no longer reachable from a test run either way. Unset (the real panel, which never sets this var) still reads the repo-root file exactly as before. This closes RISK-008's trade too: isolation is now unconditional, not declined when a file already exists. Landed after a real incident, not just the theoretical one this row described: a concurrent test run's temp override collided with the real panel process reading that same shared file at boot, and the panel refused to start. |
| RISK-010 | `check-owned-paths.ps1` **assumes worktree isolation**. When two packages legitimately coexist in the coordinator tree - which is exactly what happened here, because worktrees were blocked until the checkpoint - running it for package A flags package B's files as `UNOWNED`. Confirmed by the verifier: `-Id FND-001` flags FND-005's two files, while `-Id FND-005` passes cleanly with its own paths. | 4 | 2 | 8 | medium | coordinator | every package run outside a worktree | Not a defect in either package, and not a regression. Interpretation rule going forward: **an `UNOWNED` hit naming another active package's declared files is a tooling artifact, not an ownership violation** - confirm by re-running under that package's own id. Once packages run in separate worktrees the false positive disappears on its own. | `UNOWNED` naming a file that appears in another package's declared owned paths | accepted |

| RISK-011 | Scripted edits on Windows (Python and PowerShell defaults) emit CRLF into files git committed as LF, and `.gitattributes` has no rule for `*.md`, so git reads each carriage return as literal trailing whitespace. `git diff --check` then fails on EVERY line of a file whose visible content is otherwise unchanged. Hit for real during FND-006 on `DECISIONS.md`, and again on this very row when escape sequences in a generator script became real newlines and split one table row into four. | 4 | 2 | 8 | medium | coordinator | any package using scripted file edits | Write line endings explicitly as LF (set the newline argument in Python, or write bytes). Repair by normalizing modified files byte-wise. Avoid backslash escape sequences inside generated markdown entirely - describe them in words instead. **Consider adding a `*.md text eol=lf` rule to `.gitattributes`**, which would remove the whole class, but that is a repo-wide change outside any current package's ownership and is therefore proposed, not done. | `git diff --check` reporting trailing whitespace on lines you did not edit | open |

Statuses: open, mitigating, accepted, resolved, superseded.
